# Homework 1: Automatic Lens Smear Detection

## Running

First argument to `main.py` is the absolute path to the `sample_drive` folder.

Results will be written to `./results`

```bash
$ python main.py /path/to/sample_drive
```
